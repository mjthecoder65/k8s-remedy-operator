---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: remediationpolicies.remedy.io
spec:
  group: remedy.io
  names:
    kind: RemediationPolicy
    listKind: RemediationPolicyList
    plural: remediationpolicies
    singular: remediationpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RemediationPolicy is the Schema for the remediationpolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of RemediationPolicy
            properties:
              detectors:
                description: detectors lists which detectors are enabled within this
                  policy's scope.
                items:
                  description: DetectorConfig enables or disables a single detector
                    for this policy.
                  properties:
                    enabled:
                      default: true
                      description: enabled turns this detector on or off within the
                        policy's scope.
                      type: boolean
                    name:
                      description: name is the detector being configured.
                      enum:
                      - crashloop
                      - memorypressure
                      - certexpiry
                      - dependencytimeout
                      type: string
                  required:
                  - name
                  type: object
                type: array
              safety:
                description: |-
                  safety defines global safety limits enforced across every playbook
                  bound to this policy.
                properties:
                  blastRadiusPercent:
                    description: |-
                      blastRadiusPercent is the default per-action blast-radius cap applied
                      when a bound playbook does not set its own maxAffectedPercent.
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  circuitBreaker:
                    description: |-
                      circuitBreaker defines the default circuit-breaker behavior for
                      playbooks bound to this policy.
                    properties:
                      failureThreshold:
                        default: 3
                        description: |-
                          failureThreshold is the number of consecutive failed remediations or
                          verification failures that trips the breaker.
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  dryRun:
                    default: true
                    description: |-
                      dryRun, when true, forces every playbook under this policy into dry-run
                      mode regardless of the playbook's own setting.
                    type: boolean
                  globalRateLimit:
                    description: |-
                      globalRateLimit caps total remediation actions across all playbooks
                      bound to this policy.
                    properties:
                      maxActions:
                        description: maxActions is the maximum number of remediation
                          actions permitted within Window.
                        format: int32
                        minimum: 1
                        type: integer
                      window:
                        description: window is the rolling time window MaxActions
                          is evaluated over.
                        type: string
                    required:
                    - maxActions
                    - window
                    type: object
                type: object
              scope:
                description: scope restricts which namespaces this policy applies
                  to. Omit for cluster-wide.
                properties:
                  namespaces:
                    description: |-
                      namespaces restricts this policy to the listed namespaces. Omit or leave
                      empty for cluster-wide.
                    items:
                      type: string
                    type: array
                type: object
            type: object
          status:
            description: status defines the observed state of RemediationPolicy
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the RemediationPolicy resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
